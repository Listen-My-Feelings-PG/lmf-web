{
  "scripts": {
    "dev": "nodemon main.js",
    "start": "node main.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:reset": "psql -U postgres -f ../db/lmf_db.sql",
    "db:backup": "pg_dump -U postgres lmf_db_oneuser > ../db/backup_$(date +%Y%m%d_%H%M%S).sql",
    "clean": "rm -rf files/audio/* files/models/* files/features/* files/spectrograms/*"
  }
}